console.log("os name and version")

var a = prompt("enter a os name and version name");
console.log(a);

let func = function(a)
{
    console.log (`the OS name is  and version is ${a}`);
}
let result = func(a);
console.log(result);